import styled from 'styled-components';

import { rem } from 'utils/markup';

export const LeaguesCardsWrapper = styled.div`
  margin: ${rem(3)} 0 0;
  padding: ${rem(5)};
  display: flex;
  flex-wrap: wrap;
`;
